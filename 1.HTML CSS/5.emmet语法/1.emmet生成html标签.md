```
<h2>生成标签直接输出标签名，然后按Tab键</h2>
<h2>如果想生成多个相同标签用*，div*3然后tab</h2>
<h2>有父子关系的标签用>，ul>li然后tab</h2>
<h2>兄弟关系的标签用+，div+p然后tab</h2>
<h2>生成带有类名或者id名字的，直接.demo或者#demo然后tab</h2>
<h2>生成div类名是有顺序的，可以用自增符号$:.demo$*5</h2>
```

